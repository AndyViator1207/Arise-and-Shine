<!DOCTYPE html>
<html>

  <head>
    <title>
		<%= display_title(yield(:title)) %></title>
		<%= render 'layouts/css' %>
	</head>

  <body>
  
    <h1><%= display_title(yield(:title)) %></h1>

    <header class="navbar navbar-fixed-top navbar-inverse">
      <div class="container">
		<%= render 'layouts/header' %>
      </div>
    </header>
  
	<div class="container">
		<%= yield %>
	</div>
	
	<footer class="footer">
		<%= render 'layouts/footer' %>
	</footer>

	</body>
</html>
